<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Health Connect: läsbehörigheter (runtime-samtycke via Health Connect), epic #54 / spike #56 -->
    <!-- Kärnan (HLS-3) — Hälsa-skärmen kräver samtliga för att visa data. -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <!-- Valfria typer (HLS-8) — nekad behörighet döljer bara den datapunkten. -->
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <!-- Historik (HLS-9): utan den lämnar Health Connect bara ut 30 dagar bakåt,
         vilket tystade Trenders steg-/vilopulsdiagram för längre perioder. -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <!-- Paketsynlighet (Android 11+) så appen kan upptäcka/öppna Health Connect -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:name=".DagbokenApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dagboken"
        android:enableOnBackInvokedCallback="true">

        <!-- Disable the default WorkManager initializer so our custom HiltWorkerFactory is used -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <receiver
            android:name=".notifications.MedAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.MedActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.ScreeningReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".notifications.PeriodReminderReceiver"
            android:exported="false" />

        <!-- MY_PACKAGE_REPLACED behövs utöver BOOT_COMPLETED: systemet rensar appens
             larm vid en appuppdatering utan att skicka BOOT_COMPLETED, så utan detta
             filter var alla påminnelser borta efter varje uppdatering (NOT-14). -->
        <receiver
            android:name=".notifications.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Dagboken.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Health Connect kräver att appen deklarerar en behörighets-rationale-
                 handler, annars visas inte samtyckesdialogen (Android 13 och äldre). -->
            <intent-filter>
                <action android:name="androidx.health.connect.action.SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Health Connect på Android 14+ (API 34): behörigheternas rationale/
             användning nås via VIEW_PERMISSION_USAGE med hälso-kategorin. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

    </application>

</manifest>
